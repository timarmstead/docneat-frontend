/** @type {import('next').NextConfig} */
const nextConfig = {
  // No env block needed — Clerk pulls CLERK_SECRET_KEY at runtime in Edge
  // Optional: add other settings only if you need them (e.g. images, experimental flags)
};

export default nextConfig;